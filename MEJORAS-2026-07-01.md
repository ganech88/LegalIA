# Mejoras implementadas — 01/07/2026

## Seguridad (bloqueantes de venta, resueltos)

- **Webhook Mercado Pago con firma HMAC** (`app/api/billing/webhook/route.ts`): valida `x-signature` (ts + v1, HMAC-SHA256 con `timingSafeEqual`). Fail-closed en producción: sin `MP_WEBHOOK_SECRET` rechaza todo.
- **Sanitización anti prompt-injection** (`lib/ai/sanitize.ts`): aplicada en `generate-escrito`, `generate-escrito-custom`, `chat` y `revisar-escrito`. Los datos del usuario se limpian (control chars, backticks, tags de rol) y se tratan como datos con guard explícito en el system prompt.
- **Rate limit por IP** (`lib/rate-limit.ts`): ventana deslizante en memoria, además del límite por usuario. Escritos 15/min, chat 30/min, revisión 20/min.
- **Cuotas vía service role** (`lib/supabase/admin.ts`): `check_and_increment_usage`/`decrement_usage` se llaman con el cliente admin (fallback al del usuario si no hay service key). Migración `0018` revoca EXECUTE a `authenticated`.

## Diferenciales nuevos (lo que la competencia no tiene)

- **Verificación de citas con semáforo** (`lib/legal/citas.ts` + `components/escritos/panel-citas.tsx`): botón "Citas" en el editor. Cada artículo y fallo citado se contrasta contra el corpus: ✅ verificada / 🟡 dudosa / 🔴 no verificable (con explicación). Corre en el cliente, costo cero. Tests en `lib/legal/citas.test.ts` (lógica validada ejecutándola: 10/10 casos pasan).
- **Anexo de liquidación auditable** (`generarAnexoLiquidacion` en `lib/legal/calculadoras.ts`): norma + fórmula + importe de cada rubro, listo para acompañar la demanda. Botón "Copiar anexo auditable" en la calculadora art. 245 y se incluye en el prefill de la demanda.
- **Estilo de redacción del abogado**: campo en Configuración (`profiles.estilo_redaccion`, migración 0018) que se inyecta en los prompts de generación.
- **Corpus ampliado** (`lib/legal/corpus-complementario.ts`): +12 arts. LCT (SAC, vacaciones, enfermedad, 212, 247…), +5 CCCN (1740/41/44/48, 2561), Ley 25.323, LNE 24.013 (con aviso VERIFICAR VIGENCIA), Ley 25.345 art. 45, Teletrabajo 27.555, Ley 23.592, Ley 26.773, CN (14 bis, 17, 18). Solo síntesis fieles; nada inventado.

## Crecimiento / ventas

- **Calculadora pública SEO**: `/herramientas` y `/herramientas/indemnizacion-despido` (sin login, con metadata, CTA a registro). Habilitadas en el middleware. Link "Herramientas gratis" en la landing.
- **SEO técnico**: `app/sitemap.ts` + `app/robots.ts`.
- **Pricing nuevo** (posicionamiento vs. RivoLegal $49k / Veredicta USD 20): Free permanente con calculadoras ilimitadas · Profesional **$15.000** · Estudio **$30.000**. Actualizado en `lib/billing/plans.ts`, landing (`app/page.tsx`, `lib/landing-data.ts`) y config.

## Pasos para dejar todo productivo (requieren tu acción)

1. `pnpm install && pnpm --filter web build && pnpm --filter web test` en tu máquina (el sandbox no puede compilar).
2. Commit + push con `comitear.bat` (desde tu máquina; el sandbox no puede usar git).
3. En Vercel, cargar env vars: `MP_ACCESS_TOKEN`, **`MP_WEBHOOK_SECRET`** (nuevo — panel MP → Webhooks → clave secreta), `NEXT_PUBLIC_APP_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `CRON_SECRET`, `RESEND_API_KEY`, `EMAIL_FROM`.
4. Aplicar migración `0018_estilo_y_seguridad.sql` en Supabase **después** de configurar `SUPABASE_SERVICE_ROLE_KEY` en Vercel.
5. Configurar la URL del webhook en el panel de MP: `https://<dominio>/api/billing/webhook`.
6. Supabase free se auto-pausa por inactividad: pasar a Pro antes de vender.

## Pendientes mayores (siguiente iteración)

RAG real con embeddings/pgvector (ingestar leyes completas + fallos SAIJ) · tasas BNA/CNAT/RIPTE automáticas · integración PJN/MEV · WhatsApp para vencimientos · pay-as-you-go de escritos extra · validación de matrícula contra padrones.
